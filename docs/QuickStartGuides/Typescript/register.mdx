---
title: Register
---

# Register

## Overview

Developers can swiftly enable user account creation through Portkey SDK

## Core SDK

### type: did.register

```TypeScript
register(params: Omit<RegisterParams, 'manager'>): Promise<RegisterResult>;
```

### Usage

```TypeScript
did.register({
  type: 'Email',
  loginGuardianIdentifier: 'loginGuardianIdentifier',
  extraData: 'extraData',
  chainId: 'chainId',
  verifierId: 'verifierId',
  verificationDoc: 'verificationDoc',
  signature: 'signature',
  context: {
    requestId: 'requestId',
    clientId: 'clientId',
  },
});
```

## UI SDK

[SignIn](docs/OverviewPortkey/start-here.mdx)
