---
title: Login/Social Recovery
---

# Login/Social Recovery

## Overview

Developers can login to account through the following:

## Core SDK

### Usage

```typescript
var param = new AccountLoginParams
{
    loginGuardianIdentifier = "guardianIdentifier",
    guardiansApprovedList = guardiansApprovedList.ToArray(),
    chainId = "chainId",
    extraData = "extraData"
};
StartCoroutine(DID.Login(param, result =>
{
    // logged in
}, OnError));
```

## UI SDK

[Unity](docs/SDKs/UISDK/Unity/Unity.mdx)